# Slack Incoming Webhook URL (필수)
# 보안상 실제 URL 예시는 포함하지 않습니다.
# Slack에서 발급받은 Incoming Webhook URL을 그대로 붙여 넣으세요(절대 레포에 커밋 금지).
SLACK_WEBHOOK_URL=

# 감시할 RSS/Atom 피드 URL들(쉼표로 구분)
# 기본값: https://gsai.snu.ac.kr/feed/
FEED_URLS=https://gsai.snu.ac.kr/feed/

# 마지막으로 본 글 ID를 저장할 파일 경로
# - 로컬: .state/state.json 추천(깃에 안 올림)
# - GitHub Actions: state.json 추천(커밋으로 상태 유지)
STATE_FILE=.state/state.json

# 첫 실행(상태 파일 없음)에서 “최신 N개”를 한 번만 보내고 싶으면 설정
# 기본값 0 = 첫 실행에서는 알림을 보내지 않고 최신 글을 '기준점'으로만 저장
INITIAL_NOTIFY_COUNT=0

# Slack 메시지 1개에 최대 몇 개 글을 담을지(너무 긴 메시지/스팸 방지)
# 예: 10이면 새 글이 25개일 때 3개의 Slack 메시지로 나눠서 보냅니다.
MAX_ITEMS_PER_MESSAGE=10

# 상태가 꼬였을 때(last_seen_id가 피드에 안 보일 때) 동작
# - skip: 알림은 보내지 않고, 최신 글로 기준점을 재설정(기본, 중복 알림 방지)
# - send: 피드에 보이는 항목을 모두 “새 글”로 간주하고 알림(누락 방지, 중복 가능)
ON_STATE_MISS=skip

# SSL 검증(기본 true). 특수 환경에서만 false 권장.
VERIFY_SSL=true

# Slack으로 보내지 않고 콘솔에만 출력(테스트용)
DRY_RUN=false


